{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/style.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mt-4"> Page genre detail</h1>
        <div class="row mt-5 justify-content-center">
            <div class="col-lg-6 col-md-8 col-sm-10">
                <div class="card artist-card">
                    <div class="card-body">
                        <div class="form-group">
                            <label>Nom du genre: {{ genre.name }}
                        </div>
                        <div class="form-group">
                            <label>ID: {{ genre.id }}
                        </div>        
                        <a href="{{ path('app_genre_edit', {'id': genre.id}) }}" class="btn btn-outline-light btn-rounded mb-4">Modifier le genre</a>
                        {{ include('genre/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
        <div class="row mt-4">
            <div class="col text-center">
                <a href="{{ path('app_genre_index') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
